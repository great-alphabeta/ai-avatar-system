fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
# bcrypt used directly for password hashing — passlib 1.7.4 (2020) is
# incompatible with bcrypt 5.x and is unmaintained, so we dropped it.
bcrypt>=4.1.0
python-dotenv==1.0.0
pydantic[email]==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# Redis & Caching
redis==5.0.1
hiredis==2.3.2

# AWS
boto3==1.34.25
aioboto3==12.3.0

# WebSocket
websockets==12.0
python-socketio==5.11.0

# LLM HTTP clients (no local model weights)
# anthropic >=0.45 supports Claude 4.x models and cache_control
anthropic>=0.45.0
openai>=1.50.0

# Light image processing for avatar upload preprocessing (no torch)
numpy==1.26.3
opencv-python-headless==4.9.0.80
Pillow==10.2.0

# Celery
celery==5.3.6
flower==2.0.1

# Monitoring & Logging
prometheus-client==0.19.0
prometheus-fastapi-instrumentator==6.1.0
sentry-sdk==1.40.0

# Utilities / HTTP clients to ML microservices
httpx==0.26.0
aiofiles==23.2.1
tenacity==8.2.3
pyyaml==6.0.1
requests==2.31.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
aiosqlite==0.19.0
